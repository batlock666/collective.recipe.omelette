from setuptools import setup, find_packages

setup(name='WeWantAnotherName',
      packages=find_packages(),
      namespace_packages=['omelettetests'],
      install_requires=['setuptools'],
      )
